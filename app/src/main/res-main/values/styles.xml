<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ActivityTransitions" parent="AppTheme">
        <!--Activity transitions-->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/slide</item>
        <item name="android:windowExitTransition">@transition/slide</item>
        <item name="android:windowReenterTransition">@transition/slide</item>
        <item name="android:windowReturnTransition">@transition/slide</item>
    </style>

    <style name="SharedElement" parent="AppTheme">
        <!--Shared element transitions-->
        <item name="android:windowSharedElementEnterTransition">@transition/move</item>
        <item name="android:windowSharedElementExitTransition">@transition/move</item>
        <item name="android:windowSharedElementReenterTransition">@transition/move</item>
        <item name="android:windowSharedElementReturnTransition">@transition/move</item>
    </style>

    <style name="DialogElement">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/dialog_list_item_text_size</item>
        <item name="android:alpha">0.6</item>
        <item name="android:layout_marginTop">@dimen/dialog_margin</item>
        <item name="android:layout_marginStart">@dimen/large_margin</item>
        <item name="android:layout_marginEnd">@dimen/dialog_margin</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">marquee</item>
    </style>

</resources>
